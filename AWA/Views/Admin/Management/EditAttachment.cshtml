
<div class="card">
    <div class="form-horizontal">
        <div class="card-header ch-alt m-b-20">
            <h2>Hulpstuk '{{ctrl.attachmentDetails.name}}' wijzigen <small>Vul de gegevens hieronder in en druk vervolgens op verzenden</small></h2>
        </div>

        <div class="card-body card-padding">
            <div class="form-group">
                <label for="name" class="col-sm-2 control-label">Naam hulpstuk</label>
                <div class="col-sm-10">
                    <div class="fg-line">
                        <input required ng-model="ctrl.attachmentDetails.name" type="text" class="form-control input-sm" id="name" placeholder="Naam">
                    </div>
                </div>
            </div>
            <div class="form-group">
                <label for="number" class="col-sm-2 control-label">Nummer</label>
                <div class="col-sm-10">
                    <div class="fg-line">
                        <input required ng-model="ctrl.attachmentDetails.number" type="number" class="form-control input-sm" id="number" placeholder="Nummer">
                    </div>
                </div>
            </div>


            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button class="btn btn-primary btn-icon-text waves-effect m-r-15" ng-click="ctrl.isAdding ? ctrl.addAttachment() : ctrl.putAttachment()">
                        <i class="zmdi zmdi-check"></i>
                        {{ctrl.isAdding ? "Toevoegen" : "Opslaan"}}
                    </button>
                    <button class="btn btn-default btn-icon-text waves-effect" ng-click="ctrl.showEditCard = false; ctrl.showMainCard = true">
                        <i class="zmdi zmdi-close"></i>
                        Annuleren
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>